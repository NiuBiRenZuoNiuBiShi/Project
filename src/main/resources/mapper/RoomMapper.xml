<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.setravel.swifttravel.mapper.RoomMapper">

    <select id="findAvailableRooms" resultType="com.setravel.swifttravel.entities.Room">
        SELECT *
        FROM room
        WHERE hotel_id = #{hotelId}
            AND room_date >= #{startDate}
            AND room_date &lt; #{endDate}  AND availability = TRUE
            AND del = FALSE
            <if test="minCapacity != null">
                AND capacity >= #{minCapacity} </if>
        ORDER BY room_date, room_type, price
    </select>
</mapper>